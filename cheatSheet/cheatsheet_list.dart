void main(){

  /*

  // create a list
  List names = ['jón', 'gunnar', 'jónsson'];
  print(names);

  // change an item in a list
  names[2] = 'Karlsson';
  print(names);

  // add to a list
  names.add('helga');
  print(names);

  // add many to a list
  names.addAll(['BWM', 'auddi']);
  print(names);

  // create an empty list
  List empty = [];

  // insert at a specific index
  names.insert(5, 'nice');
  print(names);

  // insert many at a specific index
  names.insertAll(5, ['Karl', 'Johnny', 'Gunner']);
  print(names);

  // remove from a list
  names.remove('auddi');
  print(names);

  // remove at an index
  names.removeAt(6);
  print(names);


  // *BONUS* find an elements position and remove at that index
  int indexofnice =   names.indexOf('nice');
  print(indexofnice);
  names.removeAt(indexofnice);
  print(names);


  List numbs = [5,4,3,2,1,9,8,7,6];
  List chars = ['a','b','d','g','f','e'];

  // sort the list
  numbs.sort();
  print(numbs);
  chars.sort();
  print(chars);

  // remove between 2 indexes
  numbs.removeRange(2, 6);

  // remove last item
  numbs.removeLast();
  print(numbs);

   */
}